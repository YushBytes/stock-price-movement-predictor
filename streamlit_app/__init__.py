"""Streamlit research dashboard package for the Stock Price Movement Predictor.

This package provides the data-loading, charting, styling, and component
modules consumed by the top-level ``app.py`` entry point. It is a
presentation-only layer: it never modifies, retrains, or re-runs any part
of the ML pipeline located in ``src/``.
"""
